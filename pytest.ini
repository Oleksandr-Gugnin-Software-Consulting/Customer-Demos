[pytest]
markers =
    database: integration tests that require database
    messaging: messaging-related integration tests
    plugins: plugin integration tests
    api: api integration tests
    benchmark: performance benchmark tests

addopts = -ra -q
